//TODO insert the crypto libs that Igal added to the core framework so Tokens could be parsed in the client
export default function parseToken(token){
    return {
        username:'',
        displayName:''
    }
}  